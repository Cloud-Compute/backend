<?xml version="1.0" encoding="UTF8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--命名空间绑定一个指定的dao接口-->
<mapper namespace="com.hive.sell.dao.OrderMapper">

    <select id="findTopItem" resultType="Item">
        SELECT itemId AS id, SUM(payment) AS total
        FROM orders
        GROUP BY itemId
    </select>

    <select id="findTopUser" resultType="User">
        SELECT userId AS id, SUM(payment) AS total
        FROM orders
        GROUP BY userId
    </select>

    <select id="findByTime" resultType="Order">
        SELECT *
        FROM orders
        WHERE endTime > orderTime AND startTime
    </select>

</mapper>